name: KLYX E2E

on:
  workflow_dispatch:

  # KLYX_CI_SINGLE_EVENT_PHASE_12A_10
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

permissions:
  contents: read

# The authenticated E2E suite uses one dedicated KLYX account and
# changes its active profile. Serialize all runs so two PRs cannot
# race on that shared mutable test state.
concurrency:
  group: klyx-e2e-${{ github.repository }}
  cancel-in-progress: false

jobs:
  browser-tests:
    name: Playwright browser verification
    runs-on: ubuntu-22.04
    timeout-minutes: 25

    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      KLYX_E2E_EMAIL: ${{ secrets.KLYX_E2E_EMAIL }}
      KLYX_E2E_SESSION_BOOTSTRAP: "1"
      KLYX_PLAYWRIGHT_SYSTEM_CHROME: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # KLYX_CURRENT_TREE_SECRET_SCAN_PHASE_12A_9
      - name: Scan current repository for secrets
        shell: pwsh
        run: ./scripts/check-klyx-public-repo-secrets.ps1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Validate required environment
        shell: bash
        run: |
          if [ -z "$NEXT_PUBLIC_SUPABASE_URL" ]; then
            echo "NEXT_PUBLIC_SUPABASE_URL is missing"
            exit 1
          fi

          if [ -z "$NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY" ]; then
            echo "NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY is missing"
            exit 1
          fi

          if [ -z "$SUPABASE_SERVICE_ROLE_KEY" ]; then
            echo "SUPABASE_SERVICE_ROLE_KEY is missing"
            exit 1
          fi

          if [ -z "$KLYX_E2E_EMAIL" ]; then
            echo "KLYX_E2E_EMAIL is missing"
            exit 1
          fi

      - name: Install dependencies
        run: npm ci

      # KLYX_DEPENDENCY_SECURITY_GATE_PHASE_10A
      - name: Production dependency security
        run: npm audit --omit=dev --audit-level=moderate

      - name: Development dependency security
        run: npm audit --audit-level=high

      - name: Verify system Chrome
        shell: bash
        run: |
          google-chrome --version

      - name: Install Playwright FFmpeg
        env:
          PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT: "120000"
        run: npx playwright install ffmpeg

      - name: Unit and integration tests
        run: npm test

      - name: TypeScript
        run: npx tsc --noEmit --pretty false

      # KLYX_PRODUCTION_BUILD_GATE_PHASE_10A
      - name: Production build
        run: npm run build

      - name: Playwright E2E
        run: npm run test:e2e

      # KLYX_PLAYWRIGHT_SECRET_SCAN_PHASE_7D_1
      - name: Guard Playwright report secrets
        id: artifact_guard
        if: always()
        shell: bash
        run: |
          python <<'PY'
          import os
          import shutil
          import sys
          import zipfile
          from pathlib import Path

          service_role_text = os.environ.get(
              "SUPABASE_SERVICE_ROLE_KEY",
              "",
          )

          if not service_role_text:
              print(
                  "E2E service-role credential unavailable for artifact guard."
              )

              shutil.rmtree(
                  "playwright-report",
                  ignore_errors=True,
              )

              shutil.rmtree(
                  "test-results",
                  ignore_errors=True,
              )

              sys.exit(1)

          sensitive_values = [
              service_role_text.encode("utf-8"),
          ]

          roots = [
              Path("playwright-report"),
              Path("test-results"),
          ]

          leak_found = False

          def contains_secret(
              data: bytes
          ) -> bool:
              return any(
                  value and value in data
                  for value in sensitive_values
              )

          def scan_zip(
              path: Path
          ) -> bool:
              try:
                  with zipfile.ZipFile(
                      path
                  ) as archive:
                      for member in archive.infolist():
                          if member.is_dir():
                              continue

                          try:
                              data = archive.read(
                                  member
                              )
                          except Exception:
                              continue

                          if contains_secret(
                              data
                          ):
                              return True

                  return False

              except Exception:
                  return False

          for root in roots:
              if not root.exists():
                  continue

              for path in root.rglob(
                  "*"
              ):
                  if not path.is_file():
                      continue

                  try:
                      data = (
                          path.read_bytes()
                      )
                  except Exception:
                      continue

                  if contains_secret(
                      data
                  ):
                      leak_found = True
                      break

                  if zipfile.is_zipfile(
                      path
                  ):
                      if scan_zip(
                          path
                      ):
                          leak_found = True
                          break

              if leak_found:
                  break

          if leak_found:
              shutil.rmtree(
                  "playwright-report",
                  ignore_errors=True,
              )

              shutil.rmtree(
                  "test-results",
                  ignore_errors=True,
              )

              print(
                  "Sensitive E2E credential detected. "
                  "Artifacts deleted."
              )

              sys.exit(1)

          print(
              "Playwright report secret scan: PASS"
          )
          PY

      - name: Upload Playwright report
        if: ${{ always() && steps.artifact_guard.outcome == 'success' }}
        uses: actions/upload-artifact@v4
        with:
          name: klyx-playwright-report-${{ github.run_id }}
          path: playwright-report/
          if-no-files-found: ignore
          retention-days: 7
