import {
  defineConfig,
} from "vitest/config";

import path from "node:path";

// KLYX_AUTOMATED_TEST_FOUNDATION_13_29

export default defineConfig({
  test: {
    environment:
      "node",

    globals:
      false,

    include: [
      "tests/**/*.test.ts",
    ],

    exclude: [
      "node_modules/**",
      ".next/**",
      "scripts/backups/**",
    ],

    clearMocks:
      true,

    restoreMocks:
      true,

    mockReset:
      true,

    testTimeout:
      10_000,
  },

  resolve: {
    alias: {
      "@":
        path.resolve(
          __dirname,
          "."
        ),
    },
  },
});