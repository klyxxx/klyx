# KLYX — NEW CHAT STARTER

I am continuing my existing KLYX project.

Repository:
C:\Users\fenjo\Documents\klyx

Before proposing any change, read:

docs/KLYX_MASTER_STATE.md

Then continue from the exact current step described there.

Rules:

- Do not restart the project.
- Do not redesign already completed architecture without evidence.
- Full files or autonomous PowerShell patchers only.
- Exact paths required.
- After changes run:
  npm.cmd test
  npx.cmd tsc --noEmit --pretty false
  npm.cmd run build
- If I post an error, fix only that step.
- If I say "send codes", the previous step succeeded: continue.
- Keep paid OpenAI calls disabled.
- Keep development possible at 0 €.
- Never expose .env.local or secrets.
- automaticExecutionAllowed must remain false.
- Explicit confirmation remains mandatory for publication, provider selection, booking and payment.

Start by telling me the exact current KLYX step from the MASTER STATE, then continue.